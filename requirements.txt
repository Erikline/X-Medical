# requirements.txt

streamlit
opencv-python-headless  # 用于图像处理
numpy                  # ultralytics 和其他库需要
Pillow                 # ultralytics 和其他库需要
matplotlib             # <--- 添加这个！ultralytics 需要
pandas                 # ultralytics 可能需要
scipy                  # ultralytics 可能需要
seaborn                # ultralytics 可能需要（绘图相关）

# --- PyTorch (非常重要, 版本需兼容你的模型和环境) ---
# 尝试不指定版本，让 pip 自动选择，或者根据你的训练环境指定
torch
torchvision
# -------------------------------------------------
